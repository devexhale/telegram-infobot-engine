plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.5'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'io.github.jawisimo'
version = '1.0'
description = 'telegram-dialog-bots-engine'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

tasks.named('test', Test) {
    useJUnitPlatform()
}

springBoot {
    mainClass = 'io.github.jawisimo.bottemplate.BotTemplateApplication'
}