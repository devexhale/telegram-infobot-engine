// This is the root of a multi-module Gradle build.
// It intentionally contains no shared configuration — each module
// defines its own plugins, dependencies, and version independently.
//
// To build or run a specific module from the project root, target it explicitly, e.g.:
//   ./gradlew clean :telegram-infobot-engine-bot-template:build
//   ./gradlew clean :telegram-infobot-engine-bot-template:bootRun
//
// See settings.gradle for module definitions.