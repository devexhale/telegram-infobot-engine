rootProject.name = 'telegram-infobot-engine'

// Modules

include 'bot-engine'
project(':bot-engine').projectDir = file('modules/bot-engine')

include 'bot-template'
project(':bot-template').projectDir = file('modules/bot-template')